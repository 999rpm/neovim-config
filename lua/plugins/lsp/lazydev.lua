-- folke/lazydev.nvim: lua_ls completion for the Neovim API and plugin sources while editing this config.
return {
	{
		"folke/lazydev.nvim",
		ft = "lua",
		opts = {
			library = {
				{ path = "${3rd}/luv/library", words = { "vim%.uv" } },
			},
		},
	},
}
